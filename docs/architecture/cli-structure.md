# CLI Architecture

The GLITCHLAB CLI is built using a modular approach:

- `glitchlab/cli.py`: Entry point and command definitions.
- `glitchlab/identity.py`: Contains visual assets (banners) and package metadata.

## Identity Management
To update the CLI banner or version display, modify `glitchlab/identity.py`.